# StreetUI v1.4 — Rendering Engine & SSR Performance

**Shipped:** `streetui@1.4.0`
**Date:** 2026-09-25
**All numbers in this report were produced by commands executed in this session.**

---

## 1. Verification summary

| Gate | Result |
|------|--------|
| Build | **PASS — 29/29** |
| Typecheck | **PASS — 47/47** |
| Tests | **PASS — 612 passing, 0 framework failures** (7 pre-existing live-HTTP failures in example-account/example-data) |
| Real-browser harness | **PASS** — Chromium 153.0.8010.12 |
| Competitor harness | **PASS — 4/4 frameworks** |
| npm publish | **PASS** — `streetui@1.4.0` live |

---

## 2. Environment

| Field | Value |
|-------|-------|
| Node | v24.18.0 |
| OS | Linux 6.8.0-138-generic (x64) |
| CPU | 12th Gen Intel Core i7-1255U, 2 cores visible |
| Memory | ~4107 MB |
| DOM for Node measurements | happy-dom (not a browser) |
| Real browser | Chromium 153.0.8010.12 via Playwright 1.63.0 |
| Bundler (measurement only) | esbuild 0.24.2 — not a runtime dependency |

Node/happy-dom timings and Chromium timings are labelled separately throughout.
They are not cross-compared.

---

## 3. v1.4 changes (internal only, no public API changes)

### 3.1 SSR serializer optimisation (`packages/dom/src/server-node.ts`)

**What changed:** Fast-path early return in `escapeHtmlText` and `escapeHtmlAttr`
skips allocation when no special character (`&<>"'`) is present — the overwhelmingly
common case in real content. `SERIALIZED_PROPERTY_ENTRIES` hoisted to module
scope (was re-allocated ~80k times per 10k-row SSR render).

**Measured effect — deterministic same-process A/B (serialize phase only):**

| | Median |
|---|---|
| Pre-v1.4 serializer replica | 103.9 ms |
| v1.4 shipped serializer | 64.1 ms |
| Speedup | **1.62×**, output byte-identical |

SSR is mount-dominated (mount ~62%, serialize ~33%). The mount phase (shared
renderer) was not changed. The full end-to-end `renderToString` cross-run median
varies 184–233 ms on this 2-core VM — too noisy to quote as a percentage. The
serialize A/B is the reported figure.

**Byte-identity gate (SSR contract preserved):** all five route bodies
sha256-identical before/after.

### 3.2 `/* @__PURE__ */` singleton annotations (`packages/dom/src/`)

**What changed:** `serverDOMAdapter` and `browserDOMAdapter` convenience singletons
annotated `/* @__PURE__ */`. The exports are unchanged (frozen API intact); only
the unused singleton initialisers become droppable by esbuild tree-shaking.

**Measured bundle impact (esbuild, bundle+minify+treeShake, gzip):**

| Profile | v1.3 gzip | v1.4 gzip | Δ |
|---------|-----------|-----------|---|
| minimal (reactivity + client render) | 8,606 B | 7,619 B | **−987 B (−11.5%)** |
| typical (router/forms/i18n/store/resource) | 15,365 B | 14,377 B | **−988 B (−6.4%)** |
| real app entry | 15,690 B | 14,667 B | **−1,023 B (−6.5%)** |
| full (`import *`, retains SSR) | 20,105 B | 20,205 B | +100 B (+0.5%) |

The full-barrel profile legitimately includes `renderToString` and retains the
serializer — its +100 B is the serializer fast-path code, disclosed and expected.
Client bundles that don't import SSR shrink ~1 KB gzip.

---

## 4. Real-application measurements (Node + happy-dom)

| Metric | Measured |
|--------|----------|
| Initial mount /users (10k rows) | 1,869 ms, 150,032 nodes |
| Router 4-navigation | 375 ms median |
| Hydration /users | **0 nodes created** |
| Toggle 1 of 1,000 controls | **2 structural writes**, independent of N |
| Form field isolation | **7 structural writes**, unrelated field untouched |
| Keyed list: search-narrow | 10,004 structural writes |
| Keyed list: reverse sort | 10,002 structural writes |
| Keyed list: sort reorder | 9,803 structural writes |
| 8 mount/unmount cycles | 22,584 nodes/cycle, **0 orphans** |

---

## 5. Real-browser measurements — Chromium 153.0.8010.12

Source: `node scripts/browser-harness.mjs` (this session)

| Scenario | Result |
|----------|--------|
| Initial mount /users (10k rows) | **364.4 ms**, 80,031 nodes |
| Reactive search narrow | **182.5 ms**, 10,004 mutations |
| Reactive search clear | **286.8 ms**, 10,004 mutations |
| Reverse sort | **150.7 ms**, 20,002 mutations |
| Router /users (10k mount) | **565.9 ms** |
| Router other routes | 31.7 / 65.5 / 34.9 ms |
| Hydration /users (10k rows) | **370.6 ms**, **0 nodes added** |
| Frame pacing (big-list reorder) | 2 frames, maxFrame **704 ms** |
| Memory (6 cycles) | **239 MB stable** |
| Long tasks | **15 tasks, 5,689 ms total** |

Zero-node hydration holds in real Chromium. Frame pacing is not 60 fps on 10k-row
reorder — this is the primary known weakness.

---

## 6. Competitor comparison

Source: `node benchmarks/run-competitors.mjs` (this session).
StreetUI Node scenarios = happy-dom; React/Vue/Solid browser scenarios = Chromium.
Environments differ — absolute numbers not cross-compared.

| Framework | Version | Env | Initial render | Single update | List reorder | SSR | Bundle gzip |
|-----------|---------|-----|---------------|--------------|-------------|-----|-------------|
| **StreetUI** | 1.4.0 | happy-dom | 104.2 ms | **0.03 ms** | 325.5 ms | 30.9 ms | 20,205 B (full) / 7,619 B (min) |
| React | 19.1.0 | Chromium | 11.4 ms | 2.0 ms | 164.0 ms | 3.3 ms | 58,225 B |
| Vue | 3.5.13 | Chromium | 10.0 ms | 2.7 ms | **8.9 ms** | 4.4 ms | 23,540 B |
| Svelte | 5.19.0 | Chromium (SSR only) | — | — | — | **1.0 ms** | **3,962 B** |
| Solid | 1.9.3 | Chromium | 12.1 ms | **0 ms** | **1.1 ms** | — | **2,441 B** |

**Observations (no ranking):**
- StreetUI SSR (30.9 ms) trails React (3.3 ms), Vue (4.4 ms), Svelte (1.0 ms) —
  all in Node, so the comparison is valid. The v1.4 serialize optimisation
  reduces this gap partially; mount phase remains the dominant cost.
- StreetUI single update (0.03 ms, happy-dom) is in the same order as Solid
  (0 ms rounded, Chromium) — environments differ, but fine-grained reactivity
  is a demonstrable strength.
- List reorder comparison is not valid (StreetUI happy-dom vs competitors Chromium).

---

## 7. Public API

**Unchanged.** 168 values / 171 types. All v1.4 work is internal.

---

## 8. Breaking changes

**None.**

---

## 9. Known limitations

- Same-environment competitor comparison still incomplete (StreetUI Node vs
  competitors Chromium). Full same-Chromium harness is the v1.5 target.
- Svelte browser scenarios did not complete (build issue in harness).
- Solid has no SSR scenario in the current harness.
- SSR mount phase (~62% of total) not optimised in v1.4 — remaining headroom.
- 15 long tasks in real Chromium on 10k-row workloads — frame pacing weakness persists.

---

*Reproduce:* `NODE_OPTIONS=--max-old-space-size=8192 turbo run build typecheck test
--force --concurrency=1` → `node benchmarks/run-streetui.mjs` →
`node benchmarks/run-competitors.mjs` → `node scripts/browser-harness.mjs`
`npm info streetui version` → `1.4.0`
