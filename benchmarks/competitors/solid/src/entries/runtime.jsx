// DEV-ONLY benchmark artifact — H bundle entry (runtime only). NOT part of the `streetui` runtime.
import { render } from 'solid-js/web';

render(() => <div>x</div>, document.getElementById('app'));
