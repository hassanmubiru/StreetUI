{
  "name": "@streetui/example-performance-app",
  "version": "1.3.0",
  "private": true,
  "description": "StreetUI real-world performance application — header, navigation, sidebar, dashboard, a 10,000-row data table with search/filter/sort, 1,000 interactive controls, validated forms, router navigation, async resources, modal, notifications, i18n, SSR and hydration. Built entirely on the unified `streetui` API.",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "streetui": "workspace:*"
  },
  "devDependencies": {
    "typescript": "*",
    "tsup": "*",
    "vitest": "*",
    "happy-dom": "*"
  }
}
