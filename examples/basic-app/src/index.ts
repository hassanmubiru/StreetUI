/**
 * Browser entry point for the basic-app example.
 * In a real browser, call mountCounterApp(document.getElementById('app')!).
 */
export { createCounterApp, mountCounterApp } from './counter-app.js';
